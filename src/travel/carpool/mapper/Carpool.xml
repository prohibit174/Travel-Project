<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="travel.carpool.mapper.CarpoolMapper">
	<cache />
	
	<insert id="insertCarpool" parameterType="Carpool">
			insert into TEST(
			c_num, u_id, start_lati, start_longti, dest_lati, dest_longti, c_price, c_date, c_person, c_dept_time
		)
		values(
			1, 1234, #{start_lati}, #{start_longti}, #{dest_lati}, #{dest_longti}, 2000, SYSDATE, #{c_person}, SYSDATE
		)
	</insert>
	
	<select id ="listCarpool" resultType="Carpool">
	select * from TEST
	</select>
	
	
</mapper>















