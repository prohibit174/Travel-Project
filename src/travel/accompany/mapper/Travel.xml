<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="travel.accompany.mapper.TravelMapper">

	<cache />

		<select id="selectUserRoute" resultType="accompany" parameterType="String">
			select tp_num, c.cor_region, cor_longi, cor_lati, u_id, tp_date
			from cordinates c, trip_plan t where c.cor_region = t.cor_region and t.u_id='0'
		</select>
</mapper>
















